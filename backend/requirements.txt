# Core — everything except the optional extras below runs on these five.
# Verified in a clean venv: backend, CLI, reports, share and validate all import.
fastapi
uvicorn[standard]
SQLAlchemy>=2.0
python-dotenv
rich

# --- Optional extras, each needed for exactly one feature ---
# Prompt rewrites and the generated playbook (the only LLM usage):
#   pip install anthropic
# Claude desktop app extension (MCP server):
#   pip install "mcp[cli]"
# Training the MLP scorer:
#   pip install torch sentence-transformers numpy
# Postgres instead of SQLite (set DATABASE_URL):
#   pip install psycopg2-binary
